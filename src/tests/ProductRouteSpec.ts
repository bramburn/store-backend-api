import supertest from 'supertest'
import app from '../index'
const request = supertest(app)

describe("Test Product end points",()=>{
    it('should show an index of products', async function () {

    })

    // test getting information for a single product
    // test fail for getting a non-number of a product
    // test failure to create a product
    // test success to create a product


})
